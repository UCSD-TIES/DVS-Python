// main.cp#include <stdio.h>#include <windows.h>#include <wingdi.h>#include <winbase.h>#include <winuser.h>#include <process.h>#include "resource.h"#include "globals.h"#include "main.h"#if defined (WIN32)	#define IS_WIN32	TRUE#else	#define IS_WIN32	FALSE#endifIMPLEMENT_DYNCREATE(CMFCPropertiesDlg, CResizablePropertySheet)CEyeDxBooleanType CMFCPropertiesDlg::OnInitDialog(){	CResizablePropertySheet::OnInitDialog();		//CenterWindow();				// We need to add a maximize button to the frame so it's easy for the user to	// resize the window		::SetWindowLong(m_hWnd, GWL_STYLE, GetStyle() | WS_MAXIMIZEBOX);		return TRUE;}void CMFCPropertiesDlg::OnApplyNow(){}