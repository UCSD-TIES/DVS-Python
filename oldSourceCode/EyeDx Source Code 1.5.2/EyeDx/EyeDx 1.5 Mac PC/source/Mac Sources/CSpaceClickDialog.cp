// =================================================================================//	CSpaceClickDialog.cp// =================================================================================//	CSpaceClickDialog.h	#include <LStdControl.h>#include <PP_Messages.h>#include <UReanimator.h>#include "EyeDxMacConstants.h"#include "CSpaceClickDialog.h"// ---------------------------------------------------------------------------------//		¥ CSpaceClickDialog(LStream *)// ---------------------------------------------------------------------------------CSpaceClickDialog::CSpaceClickDialog(	LStream *inStream )		: LGADialog( inStream ){}// ---------------------------------------------------------------------------------//		¥ ~CSpaceClickDialog// ---------------------------------------------------------------------------------CSpaceClickDialog::~CSpaceClickDialog(){}// ---------------------------------------------------------------------------//	¥ HandleKeyPress												  [public]// ---------------------------------------------------------------------------//	DialogBox handles keyboard equivalents for hitting the Default Button. If// the input character is the ESC key, we pass it on to the base class.////		Default Button: Any key but ESCBooleanCSpaceClickDialog::HandleKeyPress(	const EventRecord&	inKeyEvent){	Boolean		keyHandled	= true;	LControl*	keyButton	= nil;	UInt8		theChar		= (UInt8) (inKeyEvent.message & charCodeMask);		if ( (theChar != char_Escape)) {		keyButton = dynamic_cast<LControl*>(FindPaneByID(mDefaultButtonID));				}				if (keyButton != nil) {		keyButton->SimulateHotSpotClick(kControlButtonPart);			} else {		keyHandled = LGADialog::HandleKeyPress(inKeyEvent);	}		return keyHandled;}