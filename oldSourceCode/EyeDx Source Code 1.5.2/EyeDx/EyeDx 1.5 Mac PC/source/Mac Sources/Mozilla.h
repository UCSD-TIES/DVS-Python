#include <stdio.h>#include <unistd.h>#include <stdlib.h>#include <string.h>extern OSErr FSSpecFromLocalUnixPath(const char * unixPath, FSSpec * inOutSpec, Boolean resolveAlias);extern OSErr FSSpecFromPathname(const char* inPathNamePtr, FSSpec* outSpec);extern void  SwapSlashColon(char * s);extern char*  MacPathFromUnixPath(const char* unixPath);extern OSErr FSSpecFromLocalUnixPath(const char * unixPath, FSSpec * inOutSpec, Boolean resolveAlias);extern char * GetURLFromFileSpec(const FSSpec& inSpec);